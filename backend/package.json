{
  "name": "attendance-backend",
  "version": "1.0.0",
  "description": "Student Attendance System - Backend API",
  "main": "server.js",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "start": "node --experimental-sqlite server.js",
    "dev": "nodemon server.js",
    "lint": "eslint . --ext .js",
    "test": "node --experimental-sqlite node_modules/.bin/jest --forceExit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.0",
    "supertest": "^7.0.0"
  }
}
